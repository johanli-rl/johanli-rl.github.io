source "https://rubygems.org"

# Use the GitHub Pages gem which pins all the dependencies needed for GitHub Pages
gem "github-pages", group: :jekyll_plugins
gem "minimal-mistakes-jekyll", "~> 4.26.2"  # Ensure the version exists